{
  "name": "ks-portfolio",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/atproto-sync.mjs",
    "preview": "astro preview",
    "atproto:init": "node scripts/atproto-sync.mjs --init",
    "playlist:sync": "node scripts/playlist-sync.mjs"
  },
  "dependencies": {
    "@emailjs/browser": "^3.11.0"
  },
  "devDependencies": {
    "astro": "^5.0.0",
    "sass": "^1.63.6"
  }
}
