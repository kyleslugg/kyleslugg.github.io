---
title: Draft template — replace me
date: 2026-06-12
blurb: A scaffold for the first essay. Drafts never appear in production builds; preview with `npm run dev`.
draft: true
---

Body text renders in the serif reading style. Write plain markdown —
paragraphs, *emphasis*, **strong**, and [links](https://kyleslugg.com)
all work.

## A section heading

Headings keep the site's monospace accent. Lists:

- One idea per line
- Another idea

> Blockquotes get a quiet left rule, for the philosophers.

Inline `code` and blocks:

```python
def legible(system):
    return explain(system)
```

Delete this file's contents, change the frontmatter, drop `draft: true`,
and the post publishes on the next push.
